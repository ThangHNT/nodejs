<div class="mt-4 container">
    <form>
        <h2>Your account</h2>
        {{#each user}}
        <div class="form-group row">
            <label for="email" class="col-sm-2 col-form-label">Email</label>
            <div class="col-sm-10">
            <input type="text" class="form-control" value="{{this.email}}" id="email" >
            </div>
        </div>
        {{/each}}
    </form>
</div>